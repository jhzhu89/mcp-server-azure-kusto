List all tables in a database with basic info (name, folder, description).

Use get-table-schema tool to understand table structure before querying. Always add filters and limits when querying tables.
